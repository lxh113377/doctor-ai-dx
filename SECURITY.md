# 安全说明（SECURITY）

> 本文件说明**这个系统的安全边界是什么、不是什么**。竞赛演示项目，未做商用安全认证；请勿按「已加固的医疗信息系统」理解它。

## 报告漏洞

优先通过 GitHub 仓库 **Security → Report a vulnerability**（私有漏洞报告）提交。请附上：复现步骤、影响的接口/视图、是否涉及病例文本外泄。

**请在公开 Issue 之外提交**涉及真实患者数据、密钥泄漏或未授权访问的问题。

## 范围内 / 范围外

| 属于本报告范围 | 不属于（当前形态本就不提供） |
|---|---|
| 诊断链路输出违反红线（红旗被模型覆盖、出现无白名单来源的引用） | 渗透测试、等保/三级认证、ONC/FDA 类合规 |
| 病例文本或密钥出现在错误响应、日志或前端可见处 | 账号体系、权限模型、多租户隔离 |
| Cloudflare Pages/Functions 配置导致的越权或数据可读 | 客户端应用（本项目只有 Web） |
| 依赖链引入的可被远程触发的执行路径 | 社会工程、账号凭据丢失 |

## 已实现的控制（可核验）

- **危险信号规则层独立于大模型**：规则先算、强制覆盖模型输出，模型无法取消或稀释红旗提示。
- **引用白名单**：所有 `evidence_id` 必须存在于知识库单一源，非法引用直接拒绝；`hybrid` 检索器输出同样逐条过白名单。
- **失败安全降级**：无密钥 / 超时 / 非法 JSON → `mode=rule-fallback` 并带 `fallback_reason`，绝不静默返回"看起来正常"的假结论。
- **错误响应零内部信息**：500 只回医生可理解文案 + 故障编号，不含堆栈、文件路径、上游响应体。
- **日志出站前脱敏**：`sk-` 形态密钥、运行环境中的 API Key 原文、内部路径统一替换后才写日志；请求体（可能含病例文本）不入日志。
- **前端不渲染知识库 `url`**：避免把尚未核验到文档级的链接暴露成"可点击的权威出处"（详见 `docs/EVAL_CARD.md` §1 溯源等级）。
- 以上由 `frontend/tests/route_guard.mjs`、`backend/tests/test_api_observe.py`、`frontend/tests/kb_guard.mjs` 在 CI 常跑守门。

## 明确的未保障项（试点前必须补齐）

- **无认证、无授权、无审计**：任何能访问该 URL 的人都可发起问诊；无操作留痕。
- **无服务端会话与存储**：不持久化病例，因此**禁止**把真实患者数据接入本系统当前形态。
- **日志留存依赖运行平台默认策略**（Cloudflare Pages 免费额度保留期短），未接集中式日志/追踪后端。
- **依赖自动更新已开启（Dependabot），但无 SAST/密钥扫描门禁**；CI 只做构建与行为契约。
- **模型输出不含临床责任**：本项目定位为辅助参考，输出不构成诊疗决策依据，执业医生承担终审责任。

## 支持版本

只在 `main` 上修复安全问题；发布以 SemVer tag 与 `CHANGELOG.md` 为准。历史 tag 不提供回溯补丁。
